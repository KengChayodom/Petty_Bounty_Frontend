/// Barrel for the app's skeleton-loading layer.
///
/// The app shows **no** loading indicators anywhere — pending state is always a
/// skeleton of the layout that is about to appear. Import this one file to get
/// the shared pieces plus the `skeletonizer` API (`Skeletonizer`, `Bone`,
/// `Skeleton`, `BoneMock`, ...).
library;

export 'package:skeletonizer/skeletonizer.dart';

export 'busy_button_label.dart';
export 'skeleton_analysis_overlay.dart';
export 'skeleton_effects.dart';
